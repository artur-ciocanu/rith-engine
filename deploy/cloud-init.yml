#cloud-config

# =============================================================================
# Rith Engine — Cloud-Init Auto-Setup
# =============================================================================
#
# Paste this into your VPS provider's "User Data" field when creating a server.
# Tested on: Ubuntu 22.04+, Debian 12+
# Works with any cloud-init compatible provider (DigitalOcean, Hetzner, Linode,
# Vultr, AWS EC2, Hostinger, etc.)
#
# What this does:
#   1. Installs Docker + Docker Compose plugin
#   2. Opens firewall ports (SSH, HTTP, HTTPS)
#   3. Creates a 2GB swapfile (helps small VPS builds avoid OOM)
#   4. Clones the repo to /opt/rith-engine
#   5. Prepares .env and Caddyfile from examples
#   6. Creates a dedicated 'rith' user (docker group only, no sudo)
#   7. Builds the Docker image (~5 min) as the rith user
#
# Note: On VPS with <2GB RAM, the docker build step can OOM without swap.
# Note: The 'rith' user has docker access but NOT sudo. For administrative
#       tasks (updates, reboots), use the default cloud user or root.
#
# After the server boots (~5-8 min), SSH in as the rith user:
#   ssh rith@your-server-ip
#   1. Edit /opt/rith-engine/.env — set your AI credentials, DOMAIN, DATABASE_URL
#   2. cd /opt/rith-engine && docker compose --profile with-db --profile cloud up -d
#   3. Open https://your-domain.com
#
# IMPORTANT: Before starting, point your domain's DNS A record to this server's IP.
#           SSH keys from the default cloud user are copied to 'rith'..
#

package_update: true
package_upgrade: true

packages:
  - curl
  - git
  - ufw

users:
  - default
  - name: rith
    gecos: Rith Engine Service User
    shell: /bin/bash
    lock_passwd: true

runcmd:
  # --- Swap (helps small VPS avoid OOM during docker build) ---
  - |
    if [ ! -f /swapfile ]; then
      fallocate -l 2G /swapfile || dd if=/dev/zero of=/swapfile bs=1M count=2048
      chmod 600 /swapfile
      mkswap /swapfile
      swapon /swapfile
      echo '/swapfile none swap sw 0 0' >> /etc/fstab
    fi

  # --- Docker ---
  - curl -fsSL https://get.docker.com | sh
  - usermod -aG docker rith

  # --- Copy SSH keys from default user to rith (so login works immediately) ---
  - |
    DEFAULT_USER=$(getent passwd 1000 | cut -d: -f1)
    if [ -n "$DEFAULT_USER" ] && [ -f /home/$DEFAULT_USER/.ssh/authorized_keys ]; then
      mkdir -p /home/rith/.ssh
      cp /home/$DEFAULT_USER/.ssh/authorized_keys /home/rith/.ssh/authorized_keys
      chmod 700 /home/rith/.ssh
      chmod 600 /home/rith/.ssh/authorized_keys
      chown -R rith:rith /home/rith/.ssh
    elif [ -f /root/.ssh/authorized_keys ]; then
      mkdir -p /home/rith/.ssh
      cp /root/.ssh/authorized_keys /home/rith/.ssh/authorized_keys
      chmod 700 /home/rith/.ssh
      chmod 600 /home/rith/.ssh/authorized_keys
      chown -R rith:rith /home/rith/.ssh
    fi

  # --- Firewall (443/udp needed for HTTP/3 QUIC via Caddy) ---
  - ufw allow 22/tcp
  - ufw allow 80/tcp
  - ufw allow 443/tcp
  - ufw allow 443/udp
  - ufw --force enable

  # --- Clone and configure (fail fast — single shell so set -e applies) ---
  - |
    set -e
    git clone https://github.com/artur-ciocanu/rith-engine.git /opt/rith-engine
    cp /opt/rith-engine/.env.example /opt/rith-engine/.env
    cp /opt/rith-engine/Caddyfile.example /opt/rith-engine/Caddyfile
    chown -R rith:rith /opt/rith-engine

  # --- Pre-pull external images (as rith, via docker group) ---
  - sudo -u rith docker pull postgres:17-alpine
  - sudo -u rith docker pull caddy:2-alpine

  # --- Build the app image as rith ---
  - sudo -u rith -H bash -c 'cd /opt/rith-engine && docker compose build'

  # --- Signal completion ---
  - |
    cat > /opt/rith-engine/SETUP_COMPLETE << 'DONE'
    ============================================
    Rith Engine server setup complete!
    ============================================

    Log in as the 'rith' user (not root):
      ssh rith@<server-ip>

    Note: the 'rith' user has docker access but no sudo. For system
    maintenance (apt upgrade, reboots), log in as the default cloud user
    or root.

    Next steps:

      1. Edit credentials and domain:
         nano /opt/rith-engine/.env

         Required:
           CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...  (or CLAUDE_API_KEY)
           DOMAIN=your-domain.example.com
           DATABASE_URL=postgresql://postgres:postgres@postgres:5432/remote_coding_agent

      2. (Optional) Set up basic auth to protect the Web UI:
         docker run caddy caddy hash-password --plaintext 'YOUR_PASSWORD'
         # Add to .env (use $$ to escape $ in hashes):
           CADDY_BASIC_AUTH=basicauth @protected { admin $$2a$$14$$<hash> }
         # Skip if using IP-based firewall rules instead.

      3. Start all services:
         cd /opt/rith-engine
         docker compose --profile with-db --profile cloud up -d

      4. Open https://your-domain.com

    Logs:     docker compose logs -f
    Health:   curl https://your-domain.com/api/health
    Docs:     https://github.com/artur-ciocanu/rith-engine
    ============================================
    DONE
  - echo "[rith-engine] Setup complete. Edit /opt/rith-engine/.env and run docker compose up."
